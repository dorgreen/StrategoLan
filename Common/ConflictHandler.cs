namespace Common
{
    
    public static class ConflictHandler
    {    
        // TODO: IMPLEMENT
        // Should return either attacker, defender or empty (should repurpose the empty cell if one of them is empty)
        // Assuming that if attacker is movable it's movment is legal
        public static ICell Handle(ICell attacker, ICell defender)
        {
            return System.NotImplementedException();
        }
    }
}